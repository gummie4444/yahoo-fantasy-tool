import deserializeUser from './deserializeUser';
import google from './google';
import local from './local';
import yahoo from './yahoo';

export { deserializeUser, google, local, yahoo };

export default {
  deserializeUser,
  google,
  local,
  yahoo
};
